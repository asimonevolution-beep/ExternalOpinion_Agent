[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma generate; npx prisma db push --accept-data-loss; node server.js"
healthcheckPath = "/health/live"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
